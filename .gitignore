# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Local config / secrets
.env

# Trade run log
trade_log.jsonl
scan_runs.jsonl
evaluation.db
evaluation.db-*
.intraday_alert_state.json*

# On-disk data cache
.cache/

# Cron output -- with EMAIL_ENABLED=0, notify.py logs full email bodies (real trade
# verdicts/proposals) here; never commit it.
cron.log

# Local bhavcopy/delivery-trend database
bhavcopy.db

# Local LLM evaluation runs (the versioned corpus remains tracked)
evals/results/
