# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Local config / secrets
.env

# Trade run log
trade_log.jsonl
scan_runs.jsonl
evaluation.db
evaluation.db-*
.intraday_alert_state.json*
.app_scheduler_state.json*
.app_scheduler_overrides.json*
.app_scheduler.lock
.app_scheduler.pid
.dashboard_server.pid
.ollama.pid
ollama.log
dashboard_server.log
scheduler_history.jsonl
dashboard.html

# On-disk data cache
.cache/

# Cron output -- with EMAIL_ENABLED=0, notify.py logs full email bodies (real trade
# verdicts/proposals) here; never commit it.
cron.log

# Local bhavcopy/delivery-trend database
bhavcopy.db

# Local LLM evaluation runs (the versioned corpus remains tracked)
evals/results/

# JSONL lines removed by cleanup.py
.archive/
